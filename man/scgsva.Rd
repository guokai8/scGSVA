% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich.R
\name{scgsva}
\alias{scgsva}
\title{GSVA function for single cell data or data.frame with expression value}
\usage{
scgsva(
  obj,
  annot = NULL,
  assay = NULL,
  slot = "counts",
  batch = 1000,
  method = "ssgsea",
  kcdf = "Poisson",
  abs.ranking = FALSE,
  min.sz = 1,
  max.sz = Inf,
  mx.diff = TRUE,
  ssgsea.norm = TRUE,
  useTerm = TRUE,
  maxRank = 1500,
  cores = 4,
  verbose = TRUE,
  sc.keep = TRUE,
  use.original = FALSE,
  ...
)
}
\arguments{
\item{obj}{The count matrix, Seurat, or SingleCellExperiment object.}

\item{annot}{Annotation object built by \code{buildAnnot()} or \code{buildMSIGDB()}.}

\item{assay}{Assay to use in GSVA analysis ('RNA','SCT' or 'Spatial' if spatial transcriptomics).}

\item{slot}{Specific assay data to get or set.}

\item{batch}{Chunk size for batch processing of large datasets. Default: 1000}

\item{method}{Enrichment method: \code{"ssgsea"} (default), \code{"gsva"},
\code{"plage"}, \code{"zscore"}, or \code{"UCell"}.}

\item{kcdf}{Character string denoting the kernel to use during the
non-parametric estimation of the cumulative distribution function of
expression levels across samples when method="gsva".
\code{"Poisson"} (default, for count data) or \code{"Gaussian"} (continuous data).}

\item{abs.ranking}{Logical flag used only when mx.diff=TRUE. Default: FALSE}

\item{min.sz}{Minimum size of the resulting gene sets. Default: 1}

\item{max.sz}{Maximum size of the resulting gene sets. Default: Inf}

\item{mx.diff}{Logical, two approaches to calculate the enrichment
statistic (ES) from the KS random walk statistic. \code{TRUE} (default)
uses max difference; \code{FALSE} uses maximum deviation.}

\item{ssgsea.norm}{Logical, whether to normalize ssGSEA scores. Default: TRUE}

\item{useTerm}{Logical, use pathway Term names (\code{TRUE}) or IDs (\code{FALSE}). Default: TRUE}

\item{maxRank}{Maximum number of genes to rank per cell (UCell method only).
Gene signatures are automatically filtered to genes present in the expression matrix,
so this is auto-adjusted when needed. Default: 1500}

\item{cores}{Number of parallel workers for computation. Default: 4}

\item{verbose}{Logical, print progress information. Default: TRUE}

\item{sc.keep}{Logical, keep the full single cell data in the result object. Default: TRUE}

\item{use.original}{Logical, if \code{TRUE} use the original GSVA Bioconductor package
implementation; if \code{FALSE} (default) use the fast custom C++ implementation.}

\item{...}{Additional arguments passed to internal scoring functions.}
}
\description{
GSVA function for single cell data or data.frame with expression value
}
\examples{
\dontrun{
set.seed(123)
library(scGSVA)
data(pbmc_small)
hsko<-buildAnnot(species="human",keytype="SYMBOL",anntype="KEGG")
res<-scgsva(pbmc_small,hsko)
}
}
\author{
Kai Guo
}
